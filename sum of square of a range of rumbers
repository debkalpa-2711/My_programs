Start_limit = int(input("enter the start limit >> "))
End_limit = int(input("enter the end limit >>"))

Sum = 0

for i in range(Start_limit, End_limit):
    calculate = pow(i, 2)
    Sum += calculate
    calculate = 0
print(f"the sum of square of the limit is {Sum}")

